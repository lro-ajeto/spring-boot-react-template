version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: spring-boot-react-template-app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    # Enable live reload of logs and restarts if the app crashes
    restart: unless-stopped
